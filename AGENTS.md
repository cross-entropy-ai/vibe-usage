## Learned User Preferences

- README should stay concise and keep advanced material in separate docs.
- Prefer operational, step-by-step writing for advanced usage docs.
- Keep supported tool mentions minimal (single-line names instead of detailed tables).
- Store documentation images under `docs/pics/` and keep links consistent.

## Learned Workspace Facts

- Advanced sync/setup guidance lives in `docs/advanced-usage.md`.
- Build instructions are split into `docs/build-from-source.md`.
- Dashboard and API endpoint docs are split into `docs/web-dashboard.md`.
- Release and architecture references are split into `docs/release.md` and `docs/architecture.md`.
